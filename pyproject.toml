[project]
name = "phare"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flare",
    "huggingface_hub>=0.20.0",
]

[tool.uv.sources]
flare = { git = "https://github.com/Giskard-AI/flare" }
